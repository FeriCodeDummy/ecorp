import React from 'react'

function MailTest(){
    const sendmail = () => {

    }

    return (
        <section>
            <h1>Mail sender</h1>
            <button className="btn btn-primary" onClick={sendmail}>Send an email</button>
        </section>
    )
}

export default MailTest;
